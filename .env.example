# Copy this file to .env and paste your real API key.
# Do not commit .env to git (it is in .gitignore).

OPENAI_API_KEY=sk-your-key-here

# Optional: override random seed (default 42). Used by run_experiment_multiple_seeds.py for reproducibility.
# EXPERIMENT_RANDOM_SEED=42
