# AI Agent tuning file (optional)
# When present, overrides defaults in experiments/config.py at load time so you can adjust behavior without changing code.
# Remove this file or leave it empty to use config.py defaults.

# Strategy update interval (every N rounds call LLM to update lambda)
# update_interval: 50

# Fairness-aware Agent slift compliance red line; below this value the prompt triggers a "severe warning"
# slift_low_threshold: 0.95

# Type 2 persona lines (3 entries, used as i%3). After editing, the 9 profit-seeking Agents rotate through them in order
# adaptive_personas:
#   - "Risk Preference: Aggressive. You prefer high bids to win more, accepting lower ROI."
#   - "Risk Preference: Conservative. You are extremely cautious, strictly avoiding low ROI."
#   - "Risk Preference: Balanced. You seek a stable balance between volume and efficiency."

# Type 3 persona lines (3 entries). After editing, the 9 fairness-aware Agents rotate through them in order
# fairness_personas:
#   - "Compliance Style: Radical. You prioritize fairness above all else, reacting strongly to any bias."
#   - "Compliance Style: Pragmatic. You try to fix fairness issues gradually while protecting profit."
#   - "Compliance Style: Strict. You follow the fairness threshold precisely."

# Uncomment and change values above to take effect; leave commented to use config.py defaults.
